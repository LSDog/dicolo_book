[build-system]
requires = ["flit_core >=3.2,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "dicolo!"
authors = [{name = "LSDog and dicolo! contributers", email = "lsdog@foxmail.com"}]
dynamic = ["version", "description"]
